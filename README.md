#Authentication
This is the basic autentication project where I haved used node express,bcrypt,nodemailer etc.
New user will be able to login.It will check if the user already exist or not.
After that if user forgets the password the user will be able to change the password safely.
